{
  "name": "context-shunt-openclaw",
  "version": "1.1.0",
  "description": "Read-only context shunt for OpenClaw: blocks oversized full reads before they run, answers questions about large sources with a configurable reader model and truthful provenance, and offers a zero-model exact inspector under a cumulative disclosure ceiling.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./index.ts",
  "files": ["index.ts", "src", "openclaw.plugin.json", "README.md", "LICENSE"],
  "openclaw": {
    "extensions": ["./index.ts"]
  },
  "dependencies": {
    "@context-shunt/core": "1.1.0"
  },
  "devDependencies": {
    "@types/node": "22.10.2",
    "typescript": "5.7.2",
    "vitest": "2.1.8"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "engines": { "node": ">=22.22.3" }
}
