# Hermes adapter

Install and uninstall instructions live in [`docs/install.md`](../../docs/install.md).
The mode matrix, including why the optional Suma post-tool mode is disabled on this host,
is in [`docs/capability-matrix.md`](../../docs/capability-matrix.md).
